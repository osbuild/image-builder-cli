name: RPM

on:  # yamllint disable-line rule:truthy
  pull_request:
    branches: ["rhel-9.8_10.2"]
  push:
    branches: ["rhel-9.8_10.2"]
  merge_group:
    types: ["checks_requested"]

jobs:
  check-spec-images-deps:
    name: "Check spec file osbuild/images dependencies"
    runs-on: ubuntu-latest
    container: registry.fedoraproject.org/fedora:latest
    env:
      # workaround for expired cert at source of indirect dependency
      # (go.opencensus.io/trace)
      GOPROXY: "https://proxy.golang.org,direct"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install dependencies
        shell: bash
        run: |
          # we have the same build-deps as the images library
          curl -sL https://raw.githubusercontent.com/osbuild/images/v0.246.0/pyproject.toml > pyproject.toml
          curl -sL https://raw.githubusercontent.com/osbuild/images/v0.246.0/test/scripts/install-dependencies | bash

      - name: Vendor dependencies
        run: go mod vendor

      - name: Check dependencies in spec file
        uses: osbuild/images@check-spec-deps-action
